kind = "web"
previewPath = "/"
title = "SmartHire"
version = "1.0.0"
id = "artifacts/smarthire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19506

[services.development]
run = "pnpm --filter @workspace/smarthire run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/smarthire", "run", "build" ]
publicDir = "artifacts/smarthire/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19506"
BASE_PATH = "/"
